import {Message} from "./shared"
//function callback(obj, sender: MessageSender, sendResp: (response: object) => Promise<void>) {
//}

browser.runtime.onMessage.addListener((data: object, sender: browser.runtime.MessageSender) => {
	//switch (data as Message) {
	//	case 
	//}
});
